// import winkNLP from 'wink-nlp';
// import model from 'wink-eng-lite-web-model';
// export const bagOfWords= (text : string) => {
//     const nlp = winkNLP(model);
//     const doc = nlp.readDoc( text );
//     const sentences = doc.sentences().out();
//     const tokenizedSentences = tokenize(sentences);

// }

// const tokenize = (text: string[]) => {

// }

// const testText = " I had a dream that I was flying over a vast ocean, the waves sparkling under the sun. Suddenly, I found myself on a beach, feeling the warm sand beneath my feet. The sky was clear, and I could see distant mountains in the horizon.";
// console.log(bagOfWords(testText));